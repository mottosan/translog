A flask application demonstrating the use of redis and flask to expose a REST API to store and query trade transaction data

To build a package, run python setup.py sdist

To install, run pip install translog-<version>.tar.gz

For sample usage and endpoints, look at the load_sample_data method in translog/cli.py